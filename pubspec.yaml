name: yayo_application
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'


dependencies:
  flutter:
    sdk: flutter 
  get: ^4.6.5
  http: ^1.1.0
  shimmer: ^3.0.0
  cached_network_image: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^2.0.0  
  mockito: ^5.4.2  
  build_runner: ^2.4.6


flutter:

  
  uses-material-design: true

  
